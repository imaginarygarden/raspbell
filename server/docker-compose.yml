services:
  db:
    image: postgres:${DB_VERSION:-17-alpine}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${POSTGRES_HOST:-127.0.0.1}:${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${DB_USER} -d $${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  postgres_data: